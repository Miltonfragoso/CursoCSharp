using System;

namespace ModificadoresDeAcesso
{
    internal class Program
    {
        static void Main(string[] args)
        {
            /*Modificadores de acesso são palavras chaves usadas para espesificar a acessibilidade de um membro ou tipo.
              * 

              Public => permite que o membro da classe pode ser acessado por : Própia classe, subclasses no assembly, classes do assembly, subclasses fora do assembly, classes fora do assembly.

              Protected internal => permite que o membro da classe pode ser acessado por : Própia classe, subclasses no assembly, classes do assembly, subclasses fora do assembly.

              Internal=> permite que o membro da classe pode ser acessado por : Própia classe, subclasses no assembly, classes do assembly.

              Protected => permite que o membro da classe pode ser acessado por : Própia classe, subclasses no assembly, subclasses fora do assembly.

              Private Protected => permite que o membro da classe pode ser acessado por : Própia classe, subclasses no assembly.

              Private => permite que o membro da classe pode ser acessado por : Própia classe.
             */
        }
    }
}
