using System;

namespace BoxingUmboxing
{
    internal class Program
    {
        static void Main(string[] args)
        {
            /*Boxing é o processo de conversão de um objeto tipo valor para um objeto tipo referência compativel
             EX: int x = 20; tipo valor memória Stack. Se Object obj = x; Object é tipo referência logo uma caixinha obj é criada na Stack apontando 
            para a Heap recebendo 20 na posição esse precesso chamado BOXING
            


            UMBOXING: É o processo inverso
            EX: int y = (int) obj vai ser criado uma variável no Stack e recebe o valor 20 que estava na Heap.
             */
        }
    }
}
