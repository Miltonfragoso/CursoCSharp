/*Delegate Func<T, TResult> é um tipo de delegado predefinido em C# que representa um método que aceita um parâmetro de entrada do tipo T e 
 * retorna um valor do tipo TResult. 
 *É amplamente utilizado para passar métodos como argumentos, especialmente em operações de consulta, 
 *manipulação de coleções e programação funcional.
 *
 *ele pode aceitar até 16 parâmetros de entrada.
 *
 *public delegate TResult Func<out TResult>();
 */
